{
  "_comment": "Copy to lead_props.json and confirm the REAL internal names + enum values via `node crm-screening.mjs props leads --grep business` and `--grep \"lead type\"`. Values below were confirmed in the sandbox (portal 50160270): Business Development is a boolean checkbox ('true', not 'Yes'); Lead Type is the standard hs_lead_type ('NEW_BUSINESS', not 'New Business'). Re-confirm before pointing at prod.",
  "business_development": { "name": "business_development", "value": "true" },
  "lead_type": { "name": "hs_lead_type", "value": "NEW_BUSINESS" }
}
