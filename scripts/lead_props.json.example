{
  "_comment": "Confirmed Lead-object property mapping for Pass B. Copy this file to lead_props.json and confirm the REAL internal names + enum values via `python3 scripts/passb_hubspot.py props leads --grep business` and `--grep \"lead type\"`. The values below are the ones confirmed in the sandbox (portal 50160270) — note Business Development is a boolean checkbox ('true'), not 'Yes', and Lead Type is the standard hs_lead_type ('NEW_BUSINESS'), not 'New Business'. Re-confirm before pointing at prod.",
  "business_development": {
    "name": "business_development",
    "value": "true"
  },
  "lead_type": {
    "name": "hs_lead_type",
    "value": "NEW_BUSINESS"
  }
}
